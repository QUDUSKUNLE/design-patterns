Facade is a structural design pattern that provides a simplified interface to a library, a framework or any other complex set of classes.

Facade provides convenient access to a particular part of the subsystem's functionality

It's easy to underestimate the complexities that happen behind the scenes when you order a pizza using a credit card.
